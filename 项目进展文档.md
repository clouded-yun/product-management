# 产品参数管理系统 - 项目进展文档

本文档记录项目的开发进展、功能实现方式和后续优化方向，方便新对话时快速了解项目状态。

## 项目概述

一个基于React + Vite的产品参数管理系统Web应用，支持自定义表格列、多界面管理、数据导入导出等功能。数据存储在浏览器LocalStorage中。

## 技术栈

- **前端框架**: React 18
- **构建工具**: Vite 4
- **数据存储**: LocalStorage
- **样式**: CSS（无UI框架）

## 项目结构

```
product_management_demo/
├── src/
│   ├── components/        # React组件
│   │   ├── Login.jsx      # 登录组件
│   │   ├── MainLayout.jsx # 主布局
│   │   ├── DataTable.jsx  # 数据表格（核心组件）
│   │   ├── ColumnEditor.jsx # 列编辑器
│   │   ├── PageManager.jsx # 界面管理
│   │   └── BackupRestore.jsx # 备份恢复
│   ├── utils/            # 工具函数
│   │   ├── storage.js    # 本地存储工具
│   │   ├── imageUtils.js # 图片处理
│   │   ├── exportUtils.js # 数据导出
│   │   ├── importUtils.js # 数据导入
│   │   └── backupUtils.js # 备份恢复
│   └── styles/           # 样式文件
├── README.md             # 详细功能说明
├── 快速开始.md          # 快速启动指南
└── GitHub使用指南.md    # GitHub使用说明
```

## 已实现功能清单

### 1. 核心功能

#### 1.1 用户权限系统
- **实现方式**: 使用LocalStorage存储用户信息
- **角色类型**: 管理员(admin) 和 普通用户(user)
- **权限控制**:
  - 管理员：可创建界面、编辑数据、导入导出、管理列定义、备份恢复
  - 普通用户：只能查看和导出数据
- **默认账号**: admin / admin123
- **文件位置**: `src/components/Login.jsx`, `src/utils/storage.js`

#### 1.2 多界面管理
- **实现方式**: 每个界面有独立的ID、名称和列配置
- **功能**: 创建、编辑、删除数据界面
- **存储**: pageConfigs数组存储在LocalStorage
- **文件位置**: `src/components/PageManager.jsx`

#### 1.3 自定义列定义
- **实现方式**: 每个界面可自定义表格列
- **列属性**:
  - `key`: 唯一标识（英文）
  - `label`: 显示名称（中文）
  - `type`: 数据类型（text/number/textarea/image/select）
  - `description`: 说明字段（可选）
  - `options`: 选择类型的选项（逗号分隔）
- **列操作**: 添加、删除、调整顺序（上移/下移）
- **文件位置**: `src/components/ColumnEditor.jsx`

#### 1.4 数据录入与编辑
- **实现方式**: 模态框表单录入
- **字段类型支持**:
  - 文本: 普通输入框
  - 数字: 支持0和空值（空值显示为NONE）
  - 多行文本: 文本域
  - 图片: 文件上传，自动压缩为base64存储
  - 选择: 下拉选项（从预设选项中选择）
- **说明提示**: 有说明的字段在标签旁显示问号图标，悬停显示说明
- **文件位置**: `src/components/DataTable.jsx`

### 2. 数据管理功能

#### 2.1 数据筛选（v1.2优化）
- **实现方式**: 筛选按钮触发，点击列名旁的🔍图标显示筛选框
- **特点**:
  - 筛选框按需显示（仅激活的列显示）
  - 实时筛选，支持多列组合筛选
  - 筛选状态独立控制
- **代码位置**: `src/components/DataTable.jsx` (handleFilterToggle, activeFilters状态)

#### 2.2 批量选择与导出
- **实现方式**: 表格第一列添加复选框
- **功能**: 
  - 全选/取消全选
  - 单独选择行
  - 导出选中数据（CSV/JSON）
- **代码位置**: `src/components/DataTable.jsx` (selectedRows状态)

#### 2.3 数据导出
- **格式支持**: CSV、JSON
- **导出范围**: 全部数据或选中数据
- **文件位置**: `src/utils/exportUtils.js`

#### 2.4 数据导入（v1.2优化）
- **格式支持**: CSV、JSON
- **智能映射**:
  - 精确匹配：匹配列名或显示名称
  - 模糊匹配：部分匹配列名或显示名称
  - 自动类型转换：根据目标列类型转换数据
- **特点**: 无需严格匹配列名，系统自动识别并映射
- **文件位置**: `src/utils/importUtils.js`, `src/components/DataTable.jsx` (handleImport函数)

#### 2.5 数据备份与恢复
- **功能**: 完整备份（用户、配置、数据）
- **用途**: 代码更新前备份，更新后恢复
- **文件位置**: `src/components/BackupRestore.jsx`, `src/utils/backupUtils.js`

### 3. 界面优化功能（v1.2新增）

#### 3.1 列宽调整
- **实现方式**: 
  - 自动调整：根据内容自动调整（浏览器默认行为）
  - 手动调整：管理员可拖拽列边界调整列宽
  - 列宽保存在LocalStorage中，按页面ID存储
- **代码位置**: `src/components/DataTable.jsx` (handleColumnResizeStart, columnWidths状态)
- **样式**: `src/styles/DataTable.css` (.column-resizer)

#### 3.2 列顺序调整
- **实现方式**: 列编辑器中提供上移/下移按钮
- **文件位置**: `src/components/ColumnEditor.jsx`

#### 3.3 筛选功能优化（v1.2）
- **优化前**: 所有列下方始终显示筛选框
- **优化后**: 点击列名旁的🔍图标才显示筛选框
- **实现方式**: activeFilters状态控制筛选框显示
- **代码位置**: `src/components/DataTable.jsx`, `src/styles/DataTable.css` (.filter-toggle-btn)

### 4. 数据存储结构

#### LocalStorage键名
- `users`: 用户列表
- `currentUser`: 当前登录用户
- `pageConfigs`: 界面配置列表
- `pageData`: 所有界面的数据（对象，key为页面ID）
- `columnWidths_${pageId}`: 列宽设置（按页面存储）

#### 数据结构示例
```javascript
// 用户
{
  id: "1",
  username: "admin",
  password: "admin123",
  role: "admin"
}

// 界面配置
{
  id: "1234567890",
  name: "产品参数",
  columns: [
    {
      key: "product_name",
      label: "产品名称",
      type: "text",
      description: "产品的中文名称",
      options: "" // 仅select类型使用
    }
  ]
}

// 数据记录
{
  id: "9876543210",
  product_name: "示例产品",
  price: 99.99,
  image: "data:image/jpeg;base64,..."
}
```

## 关键实现细节

### 1. 筛选功能实现
```javascript
// 状态管理
const [filters, setFilters] = useState({}); // 筛选值
const [activeFilters, setActiveFilters] = useState({}); // 控制筛选框显示

// 切换筛选显示
const handleFilterToggle = (colKey) => {
  setActiveFilters(prev => ({
    ...prev,
    [colKey]: !prev[colKey]
  }));
};

// 应用筛选
const applyFilters = () => {
  let result = [...data];
  Object.keys(filters).forEach(colKey => {
    const filterValue = filters[colKey];
    if (filterValue && filterValue.trim() !== '') {
      result = result.filter(row => {
        const value = String(row[colKey] || '').toLowerCase();
        return value.includes(filterValue.toLowerCase());
      });
    }
  });
  setFilteredData(result);
};
```

### 2. 列宽调整实现
```javascript
// 状态管理
const [columnWidths, setColumnWidths] = useState({});
const [resizingColumn, setResizingColumn] = useState(null);

// 开始调整
const handleColumnResizeStart = (colKey, e) => {
  const startX = e.clientX;
  const startWidth = columnWidths[colKey] || 150;
  
  const handleMouseMove = (e) => {
    const diff = e.clientX - startX;
    const newWidth = Math.max(50, startWidth + diff);
    setColumnWidths(prev => ({ ...prev, [colKey]: newWidth }));
  };
  
  // 添加事件监听...
};
```

### 3. 智能导入实现
```javascript
// 智能映射列
const columnMapping = {};
importedData.headers.forEach(importHeader => {
  // 精确匹配
  let matchedCol = columns.find(col => 
    col.key === importHeader || col.label === importHeader
  );
  
  // 模糊匹配
  if (!matchedCol) {
    matchedCol = columns.find(col => 
      col.key.toLowerCase().includes(importHeader.toLowerCase()) ||
      col.label.toLowerCase().includes(importHeader.toLowerCase())
    );
  }
  
  if (matchedCol) {
    columnMapping[importHeader] = matchedCol;
  }
});
```

## 已知问题和限制

1. **数据存储**: 使用LocalStorage，浏览器数据清除会丢失
2. **图片存储**: base64格式存储，大量图片可能导致存储空间不足
3. **安全性**: 密码明文存储，仅适合内部使用
4. **性能**: 大量数据时可能影响性能（建议<1000条记录）

## 后续优化方向

### 功能优化
1. ✅ 筛选功能优化（按钮触发）- 已完成
2. ✅ 列宽自动调整和手动调整 - 已完成
3. ✅ 导入文件智能识别 - 已完成
4. ⏳ 数据验证规则（必填、格式、范围等）
5. ⏳ 高级搜索（多条件组合搜索）
6. ⏳ 数据统计和图表展示
7. ⏳ 批量删除功能
8. ⏳ 数据排序功能

### 技术优化
1. ⏳ 后端集成（数据库存储）
2. ⏳ 用户认证增强（密码加密、JWT）
3. ⏳ 图片服务（使用OSS/CDN）
4. ⏳ 移动端适配
5. ⏳ 性能优化（虚拟滚动、分页）

### 用户体验优化
1. ⏳ 快捷键支持
2. ⏳ 拖拽排序数据行
3. ⏳ 列显示/隐藏控制
4. ⏳ 表格主题切换
5. ⏳ 操作撤销/重做

## 版本历史

### v1.2.0 (2024-01-13)
- ✅ 筛选功能优化：改为按钮触发，筛选图标放在列名末尾
- ✅ 列宽自动调整和手动调整功能（仅管理员）
- ✅ 导入文件优化：智能识别列并自动映射
- 列编辑器支持列顺序调整（上移/下移）
- 列定义支持添加说明字段（输入时显示问号提示）
- 数字类型支持0和空值（空值显示为NONE）
- 新增选择类型（下拉选项）
- 数据表格添加筛选功能
- 支持批量选择和多选导出
- 新增数据导入功能（支持CSV和JSON格式）

### v1.1.0 (2024-01-13)
- 新增数据备份与恢复功能
- 支持完整数据导出和导入
- 方便代码更新后数据迁移

### v1.0.0 (2024-01-13)
- 初始版本发布
- 实现基本的产品参数录入功能
- 支持自定义列定义和多界面管理
- 实现用户权限系统
- 支持图片上传和数据导出

## 开发环境设置

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

## 重要文件说明

### 核心组件
- **DataTable.jsx**: 数据表格组件，包含筛选、导入、导出、编辑等核心功能
- **ColumnEditor.jsx**: 列定义编辑器
- **PageManager.jsx**: 界面管理组件
- **BackupRestore.jsx**: 备份恢复组件

### 工具函数
- **storage.js**: 本地存储封装，提供统一的存储接口
- **importUtils.js**: CSV/JSON解析和导入
- **exportUtils.js**: 数据导出为CSV/JSON
- **backupUtils.js**: 完整备份和恢复

## 使用提示

1. **更新代码前**: 使用"备份/恢复"功能导出备份文件
2. **导入数据**: 系统会自动识别列，无需严格匹配列名
3. **列宽调整**: 管理员可拖拽列边界调整，设置会自动保存
4. **筛选功能**: 点击列名旁的🔍图标激活筛选
5. **批量操作**: 使用复选框选择多行，然后导出选中数据

## 联系与支持

如有问题或建议，请查看README.md获取详细文档。

---

**最后更新**: 2024-01-13  
**当前版本**: v1.2.0
